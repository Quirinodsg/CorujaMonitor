🚨 DIAGNOSTICAR API CRASHADA - URGENTE
======================================

📋 COPIE E COLE NO SSH DO LINUX:

cd /home/administrador/CorujaMonitor

echo "🔍 Verificando status dos containers..."
docker-compose ps

echo ""
echo "📋 Logs da API (últimas 50 linhas):"
docker-compose logs --tail=50 api

echo ""
echo "🔄 Tentando reiniciar API..."
docker-compose restart api

echo ""
echo "⏳ Aguardando 20 segundos..."
sleep 20

echo ""
echo "✅ Testando API novamente:"
curl -I http://localhost:8000/health

echo ""
echo "📋 Se ainda não funcionar, ver logs completos:"
echo "docker-compose logs api | tail -100"
