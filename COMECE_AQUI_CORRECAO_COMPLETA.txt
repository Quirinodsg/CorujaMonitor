═══════════════════════════════════════════════════════════════
  🚀 COMECE AQUI - CORREÇÃO COMPLETA PING
  📅 11 MARÇO 2026
═══════════════════════════════════════════════════════════════

## 🎯 OBJETIVO

Corrigir duplicação de sensores PING e desabilitar PING na probe.

## ✅ CORREÇÕES JÁ APLICADAS NO CÓDIGO

1. worker/tasks.py - Busca sensor sem filtrar nome, normaliza para PING
2. probe/probe_core.py - PING desabilitado (_collect_ping_only retorna)

## 📋 CHECKLIST DE EXECUÇÃO

### PARTE 1: GIT (NOTEBOOK WINDOWS)

```bash
cd ~/Coruja\ Monitor

git add worker/tasks.py probe/probe_core.py
git add COPIAR_PROBE_CORE_SRVSONDA001_AGORA.txt
git add COMECE_AQUI_CORRECAO_COMPLETA.txt
git add EXECUTAR_AGORA_CORRECAO_PING.txt
git add PROBLEMA_DUPLICACAO_PING_RESOLVIDO.md
git add LIMPAR_SENSORES_COMPLETO_AGORA.txt

git commit -m "fix: Corrigir duplicação PING - worker normaliza, probe desabilitada"
git push origin master
```

### PARTE 2: ATUALIZAR SRVSONDA001 (WINDOWS)

Conectar via RDP na SRVSONDA001:

```powershell
cd "C:\Program Files\CorujaMonitor\Probe"
git pull origin master

# Verificar atualização
type probe_core.py | findstr "DESABILITADO"

# Reiniciar serviço
net stop CorujaProbe
timeout /t 5
net start CorujaProbe

# Ver logs
type probe.log | findstr /i "ping"
```

### PARTE 3: ATUALIZAR LINUX (SRVCMONITOR001)

```bash
cd /home/administrador/CorujaMonitor
git pull origin master

# Reiniciar worker
docker-compose restart worker
sleep 10
docker logs coruja-worker --tail 50 | grep -i ping

# Reiniciar API
docker-compose restart api
sleep 10
docker logs coruja-api --tail 20
```

### PARTE 4: LIMPAR SENSORES DUPLICADOS (LINUX)

```bash
# Diagnosticar
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "SELECT srv.hostname, s.id, s.name, COUNT(m.id) as metricas FROM servers srv JOIN sensors s ON s.server_id = srv.id LEFT JOIN metrics m ON m.sensor_id = s.id WHERE s.sensor_type = 'ping' GROUP BY srv.hostname, s.id, s.name ORDER BY srv.hostname, metricas DESC;"

# Anotar IDs dos sensores com MENOS métricas
# Exemplo: Se IDs são 40, 41, 42 - deletar 41 e 42

# SUBSTITUA 41,42 pelos IDs corretos!
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM remediation_logs WHERE incident_id IN (SELECT id FROM incidents WHERE sensor_id IN (41, 42));"
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM incidents WHERE sensor_id IN (41, 42);"
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM metrics WHERE sensor_id IN (41, 42);"
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM sensors WHERE id IN (41, 42);"

# Verificar (deve mostrar 1 por servidor)
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "SELECT srv.hostname, COUNT(s.id) as total_ping FROM servers srv JOIN sensors s ON s.server_id = srv.id WHERE s.sensor_type = 'ping' GROUP BY srv.hostname;"
```

### PARTE 5: DELETAR SENSORES DESCONHECIDOS (LINUX)

```bash
# Ver sensores unknown
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "SELECT id, name, sensor_type FROM sensors WHERE sensor_type = 'unknown' OR name LIKE '%unknown%';"

# Deletar em cascata
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM remediation_logs WHERE incident_id IN (SELECT id FROM incidents WHERE sensor_id IN (SELECT id FROM sensors WHERE sensor_type = 'unknown' OR name LIKE '%unknown%'));"
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM incidents WHERE sensor_id IN (SELECT id FROM sensors WHERE sensor_type = 'unknown' OR name LIKE '%unknown%');"
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM metrics WHERE sensor_id IN (SELECT id FROM sensors WHERE sensor_type = 'unknown' OR name LIKE '%unknown%');"
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "DELETE FROM sensors WHERE sensor_type = 'unknown' OR name LIKE '%unknown%';"

# Verificar (deve ser 0)
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "SELECT COUNT(*) FROM sensors WHERE sensor_type = 'unknown';"
```

### PARTE 6: FECHAR INCIDENTES DE PING (LINUX)

```bash
# Fechar incidentes abertos
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "UPDATE incidents SET status = 'resolved', resolved_at = NOW(), resolution_notes = 'Fechado automaticamente - PING migrado para worker' WHERE sensor_id IN (SELECT id FROM sensors WHERE sensor_type = 'ping') AND status != 'resolved';"

# Verificar (deve ser 0)
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "SELECT COUNT(*) FROM incidents i JOIN sensors s ON i.sensor_id = s.id WHERE s.sensor_type = 'ping' AND i.status != 'resolved';"
```

### PARTE 7: VERIFICAÇÃO FINAL (LINUX)

```bash
# Contar sensores PING (deve ser 1 por servidor)
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "SELECT srv.hostname, COUNT(s.id) as total_ping FROM servers srv JOIN sensors s ON s.server_id = srv.id WHERE s.sensor_type = 'ping' GROUP BY srv.hostname;"

# Ver latências atuais
docker exec -it coruja-postgres psql -U coruja -d coruja_monitor -c "SELECT srv.hostname, m.value as latencia_ms, m.status, m.timestamp FROM metrics m JOIN sensors s ON m.sensor_id = s.id JOIN servers srv ON s.server_id = srv.id WHERE s.sensor_type = 'ping' ORDER BY m.timestamp DESC LIMIT 10;"

# Ver logs worker
docker logs coruja-worker --tail 100 | grep -i "ping"
```

## ✅ RESULTADO ESPERADO

```
hostname        | total_ping
----------------+------------
SRVCMONITOR001  |          1
SRVSONDA001     |          1
```

- 1 sensor PING por servidor (nome: PING)
- 0 sensores desconhecidos
- 0 incidentes de PING abertos
- Latências atualizando a cada 60s
- Worker logando: "🏓 Iniciando PING..."
- Probe logando: "⚠️ PING desabilitado"

## 📚 ARQUIVOS DE REFERÊNCIA

- COMECE_AQUI_CORRECAO_COMPLETA.txt (este arquivo)
- COPIAR_PROBE_CORE_SRVSONDA001_AGORA.txt (copiar probe)
- EXECUTAR_AGORA_CORRECAO_PING.txt (comandos detalhados)
- PROBLEMA_DUPLICACAO_PING_RESOLVIDO.md (documentação)
- LIMPAR_SENSORES_COMPLETO_AGORA.txt (limpeza completa)

## 🎉 PRONTO!

Após executar todos os passos, o sistema estará 100% funcional com PING direto do servidor Linux, sem duplicação!

═══════════════════════════════════════════════════════════════
